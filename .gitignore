# --- OS / editor noise --------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
*~
.idea/
.vscode/
.cursor/

# --- Node / build outputs -----------------------------------------------------
node_modules/
dist/
build/
.vite/
.cache/
*.tsbuildinfo

# --- Python -------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.ipynb_checkpoints/

# --- Logs / runtime -----------------------------------------------------------
logs/
*.log
npm-debug.log*

# --- Archives (use GitHub Releases instead) -----------------------------------
# Downloadable project bundles live in GitHub Releases, not in the repo.
# See https://github.com/alovladi007/louis-antoine-portfolio/releases
*.zip
*.tar.gz
*.tgz
*.7z

# --- Botched git operations ---------------------------------------------------
# Catches a /git/ working-tree copy or stray loose objects from a prior incident.
/git/

# --- Secrets / env ------------------------------------------------------------
.env
.env.*
!.env.example
secrets.json
credentials.json
*.pem
*.key

# --- Backend model weights (large, fetch separately) --------------------------
backend/model/*.weights
backend/model/*.onnx
backend/model/*.pt
backend/model/*.h5
