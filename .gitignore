# --- OS / editor noise --------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
*~
.idea/
.vscode/
.cursor/

# --- Node / build outputs -----------------------------------------------------
node_modules/
dist/
build/
.vite/
.cache/
*.tsbuildinfo

# --- Python -------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.ipynb_checkpoints/

# --- Logs / runtime -----------------------------------------------------------
logs/
*.log
npm-debug.log*

# --- Archives (use GitHub Releases instead) -----------------------------------
*.zip
*.tar.gz
*.tgz
*.7z
# Exceptions: zips still wired as download buttons on project pages.
# Migrate these to GitHub Releases, then remove the exceptions.
!projects/pcm-gst-research.zip
!riscv-soc-files/docs/riscv_soc_paper_assets.zip

# --- Botched git operations ---------------------------------------------------
# Catches a /git/ working-tree copy or stray loose objects from a prior incident.
/git/

# --- Secrets / env ------------------------------------------------------------
.env
.env.*
!.env.example
secrets.json
credentials.json
*.pem
*.key

# --- Backend model weights (large, fetch separately) --------------------------
backend/model/*.weights
backend/model/*.onnx
backend/model/*.pt
backend/model/*.h5
